
module.exports = class Course {
    constructor(
       course_id, //cid Course ID
       name,
       work
    ) {
       this.course_id = course_id;
       this.name = name;
       this.work = work;
    }
}